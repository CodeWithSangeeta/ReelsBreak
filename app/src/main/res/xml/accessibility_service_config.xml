<?xml version="1.0" encoding="utf-8"?>
   <!--
       ✅ BANKING APP FIX — Accessibility Service Configuration
       =========================================================
       KEY CHANGES made to prevent banking app false positives:

       1. android:packageNames — MOST IMPORTANT FIX
          Restricts your service to ONLY watch these specific apps.
          Banking app scanners check this field. If it's empty (watches
          ALL apps), the scanner flags it as a potential keylogger/spy.
          Now it explicitly says: "I only watch YouTube, Instagram, etc."

       2. android:accessibilityEventTypes — reduced to minimum needed
          Only TYPE_WINDOW_STATE_CHANGED, TYPE_VIEW_SCROLLED, and
          TYPE_WINDOW_CONTENT_CHANGED. No broad event listening.

       3. android:canRetrieveWindowContent="true"
          Required for your detection logic (reading view IDs).
          Keep as true — this is declared openly and honestly.

       4. android:accessibilityFeedbackType="feedbackGeneric"
          Minimal feedback type — does not claim haptic/audio/visual
          feedback which would raise more flags.
   -->
<accessibility-service
xmlns:android="http://schemas.android.com/apk/res/android"

android:accessibilityEventTypes="typeWindowStateChanged|typeViewScrolled|typeWindowContentChanged"

android:accessibilityFeedbackType="feedbackGeneric"

android:accessibilityFlags="flagDefault"

android:canRetrieveWindowContent="true"

android:description="@string/accessibility_service_description"

android:notificationTimeout="100"

android:packageNames="
        com.google.android.youtube,
        com.instagram.android,
        com.snapchat.android,
        com.facebook.katana,
        com.facebook.lite,
        com.zhiliaoapp.musically"

android:settingsActivity=".MainActivity" />